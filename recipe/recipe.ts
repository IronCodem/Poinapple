interface Recipe {
  1, 2, 3, 4: string;
}

function greeter(Recipe: recipe) {
  return "Recipe of obtaining the perfect pinnapple, " + recipe.1 + ", " + recipe.2 + ", "; + recipe.3 + ", " + recipe.4 + ".";
}

let recipe = {
  1: "1. Get a pinnapple",
  2: "2. Slice it up"
  3: "3. Done"
  4. "4. Eat it"
};
